<li>
  <span class="text-muted">
    {{ post.published }}
  </span>
  <a href="{{ post.url }}">
    {{ post.title }}
  </a>
  {% if len(post.other_posts_in_sequence) > 0 %}
    <span class="text-muted">({{ 1 + len(post.other_posts_in_sequence) }})</span>
  {% endif %}
</li>