{% extends "base.html.j2" %}
{% block content %}
  <ul>
    {% for pep in peps %}
      <li>
        <span class="text-muted">
          {{ pep.slug.upper() }}
        </span>
        {% if len(pep.posts) == 1 %}
          <a href="{{ pep.posts[0].url }}">
            {{ pep.title }}
          </a>
        {% else %}
          {{ pep.title }}:
          <ul>
            {% for post in pep.posts %}
              <li>
                <a href="{{ post.url }}">
                  {{ post.title }}
                </a>
              </li>
            {% endfor %}
          </ul>
        {% endif %}
      </li>
    {% endfor %}
  </ul>
{% endblock %}
